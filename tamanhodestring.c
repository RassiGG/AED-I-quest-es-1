#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#define tam 150
///*Faça um programa que leia uma string de tamanho definido pelo programador, 
//receba o input por um usuário, pegue o tamanho da string
//e crie uma variável nova com o tamanho específico do texto digitado pelo usuário,
// copiando o texto da string 
//original na variável nova e depois printe o texto da variável 
//nova e compare o tamanho da variável original com a variável nova.
int main(){
        char texto1[50];
        printf("Digite um texto");
        scanf("%s", texto1);
    return 0;
}